# 会议室预约系统真实浏览器测试报告

执行时间：2026-05-17 13:37

测试方式：Microsoft Edge + Playwright Local，真实访问前端 `http://127.0.0.1:5172` 与后端 `http://127.0.0.1:8081`。

## 结论

本轮按 `完整功能测试清单.md` 抽取 P0/P1 主链路做真实点击验证，共执行 20 项，最终 20 项通过，0 项失败。

| 批次 | 范围 | 结果 |
|---|---|---|
| 主链路 E2E | 登录异常、普通用户预约创建、管理员审批、通知发布、日历入口回归、管理端页面、AI 助手 | 11/11 通过 |
| 补充回归 | 403/404、会议室搜索、维护房间限制、日历切换、我的预约切换、普通用户通知权限、审核校验、紧急会议校验、统计切换 | 9/9 通过 |

## 关键通过项

| 清单范围 | 实测链路 | 结果 |
|---|---|---|
| 登录与权限 | 未登录访问业务页跳转登录；错误验证码提示“验证码错误” | 通过 |
| 普通用户菜单 | 登录后展示用户功能，不展示管理端菜单 | 通过 |
| 会议空间 | 搜索会议室、点击可预约房间打开新建预约弹窗、空主题校验 | 通过 |
| 预约创建 | 普通用户创建 `浏览器实测预约-20260517053246`，接口返回成功 | 通过 |
| 预约审核 | 管理员搜索该预约并点击“通过预约” | 通过 |
| 预约日历 | 管理员日历页不展示“创建/新建紧急会议”入口；周/月切换正常 | 通过 |
| 通知中心 | 管理员从顶部铃铛发布通知；普通用户可收到；普通用户无发布入口 | 通过 |
| AI 助手 | 查询消息正常返回；创建预约进入补参卡；未出现旧“当前消息服务暂时不可用”；不显示“参会人数”手填项 | 通过 |
| 管理端 | 会议室新增弹窗、设备新增弹窗、设备绑定统计、统计分析可访问 | 通过 |
| 表单校验 | 驳回预约必须填写原因；紧急会议空表单预览提示主题必填 | 通过 |
| 错误页 | 不存在路由展示错误页并提供“回到首页”按钮 | 通过 |

## 生成文件

| 文件 | 用途 |
|---|---|
| `codex-work/real-browser-e2e-test.cjs` | 主链路真实点击脚本 |
| `codex-work/real-browser-e2e-result-latest.json` | 主链路最新 JSON 结果 |
| `codex-work/real-browser-extra-regression.cjs` | 补充回归真实点击脚本 |
| `codex-work/real-browser-extra-result-latest.json` | 补充回归最新 JSON 结果 |
| `codex-work/real-e2e-20260517053246-*.png` | 主链路截图证据 |
| `codex-work/real-extra-20260517053646-*.png` | 补充回归截图证据 |

## 测试数据说明

真实点击执行期间写入了带 `浏览器实测` 前缀的预约与通知数据，保留作测试证据：

- 最终主链路预约：`浏览器实测预约-20260517053246`，已由管理员通过。
- 最终主链路通知：`浏览器实测通知-20260517053246`，普通用户已验证可见。
- 中间调试运行也可能留下同前缀测试预约/通知，可按前缀清理。

## 未覆盖范围

本轮不是 296 条清单的全量逐项点击，而是优先覆盖阻断级和高频链路。尚未逐项覆盖的内容包括：全部设备 CRUD 深层异常、全部统计图数据准确性、所有 AI 助手 8.x 接口级样本、移动端响应式逐屏截图、并发预约竞争、后端禁用 AI fallback 环境。
